package cz.cvut.arfittingroom.service

import android.content.Context
import cz.cvut.arfittingroom.draw.model.element.Element
import cz.cvut.arfittingroom.model.to.ActionTO
import java.util.UUID

class HistoryDeserializerService {

    private fun deserializeHistory(to: List<ActionTO>) {
        val elementPoll = HashMap<UUID, Element>()

    }


    private fun loadImage(context: Context, resourceId: Int) {

    }
}