package cz.cvut.arfittingroom.model.enums

/**
 * Determinate, where model/texture should be placed
 *
 */
//TODO resolve, multiple or replacebale
enum class ENodeType {
    MAKEUP,
    EYES,
    EARS,
    TOP_HEAD,
}